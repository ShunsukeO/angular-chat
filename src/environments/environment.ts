// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAi7NlS74M8efC9TpqAwYOW48xlP6Zu7r0",
    authDomain: "angular-chat-cd58c.firebaseapp.com",
    databaseURL: "https://angular-chat-cd58c.firebaseio.com",
    projectId: "angular-chat-cd58c",
    storageBucket: "angular-chat-cd58c.appspot.com",
    messagingSenderId: "376522710875"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
